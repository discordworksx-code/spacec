import React from 'react';

export default function Sidebar({ activeTab, onSelectTab, user, onLogout }) { const tabs = [['api_build', 'API Build'], ['devices', 'Devices'], ['building', 'Building'], ['settings', 'Settings']]; return <aside className="flex h-full flex-col border-r border-white/10 bg-[#0b0b0e] p-4"><div className="mb-8 text-lg font-semibold text-white">Space</div><nav className="space-y-1">{tabs.map(([id, label]) => <button key={id} onClick={() => onSelectTab(id)} className={`w-full rounded-lg px-3 py-2 text-left text-sm ${activeTab === id ? 'bg-white text-black' : 'text-zinc-400 hover:bg-white/5'}`}>{label}</button>)}</nav><div className="mt-auto space-y-3 text-xs text-zinc-500"><div>{user?.username || 'Operator'}</div><button onClick={onLogout} className="text-zinc-300">Log out</button></div></aside>; }
